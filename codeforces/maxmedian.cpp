#include <iostream>
#include <algorithm>
#include <set>

using namespace std;

int main(){
    int k,n;
    
    cin>>n>>k;
    
    int nums[n];
    for(int i = 0; i < n; i++){
        cin>>nums[i];
    }


    multiset<int> sorted;

    for(int i = )

    // int maxMed[n+1];
    // maxMed[n] = nums[n / 2];
    // for(int i = n - 1; i >= 1; i--){
    //     cout<<maxMed[i+1]<<endl;
    //     if(i % 2 == 0)
    //         maxMed[i] = max(maxMed[i+1], nums[(i-1)/2]);
    //     else
    //         maxMed[i] = max(maxMed[i+1],nums[n-1 - i / 2]);
    //     if(i == k){
    //         cout<<maxMed[k];
    //         return 0;
    //     }
    // }
}